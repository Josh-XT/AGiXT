version: "3.8"
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
  backend:
    build:
      context: ./
      dockerfile: Dockerfile-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
