version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile-streamlit
      platform: linux/amd64
    init: true
    ports:
      - "8501:8501"
    volumes:
      - ./agents:/app/agents:rw
      - ./WORKSPACE:/app/WORKSPACE:rw
      - ./models:/model:rw
    restart: always
